# Job Jugaad — context

AI-first job apply venture for Kingdom.

## Rules

- Resumes are **read-only**. Never edit, rewrite, or regenerate Desktop resume files. Pick the best track for each JD and upload that file as-is.
- Browser is **headed only** (`headless: false`). On CAPTCHA/Cloudflare, pause and wait for the human.
- Secrets (Gmail username/App Password, API keys) are **prompted every run** — never written to `.env`, git, or Kingdom `brain/`.
- LLM/text via OmniRoute `http://localhost:20128`. Web fetch via OmniRoute Firecrawl provider. No Cloudflare bypass kits.

## Layout

See `README.md`. Kingdom venture id: `job-jugaad`. Agent: `agent-jugaad`.
