---
type: overview
updated: 2026-08-24
tags: [cloudflare, tunnels, dashboards]
---

# Cloudflare dashboard links

Auto-generated by `./scripts/cloudflare-dashboards.sh` at **2026-08-24T19:04:45Z**.

This script **starts locals first**, then opens detached quick tunnels.

Readiness uses Cloudflare DoH (not system DNS) so early NXDOMAIN probes do not poison the resolver for ~30 minutes.

Quick tunnels die on reboot or if you kill the cloudflared pid. Re-run for fresh URLs.

If a link shows NXDOMAIN in the browser, wait a minute or flush DNS (`dscacheutil -flushcache`) — do not hammer-refresh.

| Dashboard | Local | Status | Public URL |
|-----------|-------|--------|------------|
| kingdom | `:5173` | UP | https://artists-calgary-ecommerce-hotels.trycloudflare.com |

## Commands

```bash
cd ~/Projects/avinashs-kingdom
./scripts/cloudflare-dashboards.sh          # start all + tunnel all
./scripts/cloudflare-dashboards.sh kingdom city comic
npm run tunnels
```

