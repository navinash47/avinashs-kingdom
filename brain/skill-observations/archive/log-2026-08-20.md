# Skill Observation Log (archive 2026-08-20)

Observations captured during task-oriented work.

**Status key:** OPEN = not yet actioned | ACTIONED (YYYY-MM-DD) = skill updated/created | DECLINED (YYYY-MM-DD) = user decided not to pursue —
resolved statuses always carry their resolution date

---

### Observation 2: sync-kingdom STATUS writes fail under sandbox

**Status:** ACTIONED (2026-08-16) — skill now requires full filesystem access for sibling STATUS writes
**Date:** 2026-08-16
**Session context:** Sync Kingdom from venture repos and start dashboards
**Skill:** sync-kingdom
**Type:** internal
**Phase/Area:** Command / permissions

**Issue:** First `npm run sync` updated panel JSON but skipped STATUS.md writes in sibling repos (WhatsApp, ProceduralCity, ComicMainEngine) with EPERM. Re-run with unrestricted filesystem access succeeded.

**Suggested improvement:** In the Command section, say the sync must run with full filesystem access (not the workspace sandbox) because it patches STATUS.md outside avinashs-kingdom.

**Principle:** A sync that writes into sibling repos must document the permission boundary, or the panel updates while source STATUS files stay stale.

### Observation 3: Comic V1 task count hides Version 2 / 2A work

**Status:** ACTIONED (2026-08-16) — sync reads v2a_program.json; panel is 2A 1/6 not V1 100%
**Date:** 2026-08-16
**Session context:** Sync Kingdom from venture repos and start dashboards
**Skill:** sync-kingdom
**Type:** internal
**Phase/Area:** Progress formula / comic-engine

**Issue:** Panel reports ComicMainEngine 100% from usage.db V1 tasks (22/22), while today's commits started Version 2A (storyboard-first) beside frozen Version 2. STATUS next-line still says "V1 board complete — Version 2 begins at phase12".

**Suggested improvement:** Extend the comic progress source to also read v2_program.json / v2a_program.json (or at least surface "V2A in progress" in version/nextMilestone) so a finished V1 board does not look like the venture is done.

**Principle:** When a venture starts a parallel program, the old completion metric must not stay the only progress source.

### Observation 4: Checking a venture via Kingdom STATUS is circular after sync

**Status:** ACTIONED (2026-08-16) — skill requires reading each repo source of truth after sync
**Date:** 2026-08-16
**Session context:** User said Version 2A was started and the prior check had not actually looked at Comic
**Skill:** sync-kingdom
**Type:** open-source
**Phase/Area:** Post-sync review

**Issue:** After `npm run sync`, the agent "checked" Comic by reading STATUS.md / ventures.json that the sync itself had just written (V1 22/22). The real program (`data/v2a_program.json`, `/v2a` dashboard, today's A0 commits) was not opened until the user corrected it.

**Suggested improvement:** After sync, verify each active venture from its own source of truth (phases.json, v2a_program.json, usage.db, applications.json), not from the STATUS line the sync just patched.

**Principle:** A generated status file is not evidence that the underlying project was inspected.
