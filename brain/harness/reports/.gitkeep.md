# Judge reports (gitignored payloads OK)

Runtime artifacts from `npm run brain:judge` land here (`judge-*.json`, `proposals/`).
Keep this directory; do not commit secrets. Fixture golden lives under `fixtures/judge-conflict/`.
